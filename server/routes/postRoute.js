const express = require('express')
const router = express.Router()

// //Import Controllers Methods:
//  const { listCheeses} = require('../controllers/postController')

// //router.get('/post', create)
// // router.post('/post', createCreate);

// router.get('/posts', listCheeses);

// router.get('/post/:id', readCheese);

// router.put('/post/:id', updateCheese);

// router.delete('/post/:id', removeCheese); //can't use delete as a controller name because it is a resered name in Js.






// module.exports = router